-- Add date field, title field and link to system user
ALTER TABLE `customer_communications` ADD COLUMN `date` datetime AFTER `text`, CHANGE COLUMN `created` `created` datetime NOT NULL AFTER `date`, CHANGE COLUMN `modified` `modified` datetime DEFAULT NULL AFTER `created`, CHANGE COLUMN `deleted` `deleted` datetime DEFAULT NULL AFTER `modified`;
ALTER TABLE `customer_communications` ADD COLUMN `user_id` int AFTER `customer_id`, CHANGE COLUMN `type` `type` varchar(20) NOT NULL AFTER `user_id`, CHANGE COLUMN `text` `text` text DEFAULT NULL AFTER `type`, CHANGE COLUMN `date` `date` datetime DEFAULT NULL AFTER `text`, CHANGE COLUMN `created` `created` datetime NOT NULL AFTER `date`, CHANGE COLUMN `modified` `modified` datetime DEFAULT NULL AFTER `created`, CHANGE COLUMN `deleted` `deleted` datetime DEFAULT NULL AFTER `modified`;
ALTER TABLE `customer_communications` ADD COLUMN `title` varchar(255) AFTER `type`, CHANGE COLUMN `text` `text` text DEFAULT NULL AFTER `title`, CHANGE COLUMN `date` `date` datetime DEFAULT NULL AFTER `text`, CHANGE COLUMN `created` `created` datetime NOT NULL AFTER `date`, CHANGE COLUMN `modified` `modified` datetime DEFAULT NULL AFTER `created`, CHANGE COLUMN `deleted` `deleted` datetime DEFAULT NULL AFTER `modified`;